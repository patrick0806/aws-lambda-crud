
const { expect } = require("@jest/globals");
const { it } = require("jest-circus");
const { describe } = require("yargs");
const api = require("../api");

describe("test crud worker api",()=>{

    it("should create user",async () => {
        
    })
})